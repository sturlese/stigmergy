"""`stigmergy.server` — the one API: the MCP server over the hybrid index and the capture queue,
with identity, ACL, rate limiting and auditing enforced server-side over both transports. It
queues and attributes captures and files nothing — filing is the librarian's, via the queue."""
