# docs — how the platform works and why

Four kinds of document, answering different questions:

| Where | Answers |
|---|---|
| [`decisions/`](./decisions) | **why** a mechanism is built the way it is — the ADRs |
| [`reference/`](./reference) | **what** each stage does, in prose, for someone operating or reading it |
| `src/stigmergy/*/index.md` | **where** things live: module by module, one map per package |

**Read the ADR before changing the mechanism it describes.** Each one records a choice that
cost something to reach — code vetoing an agent's diff instead of an LLM reviewing an LLM,
bounded agency instead of open-ended tool loops, one enforcement point for access instead of
per-surface checks. Changing the code without reading the ADR usually means rediscovering the
option that was already rejected.

## Reference docs (what each stage does)

| Doc | Covers |
|---|---|
| [`reference/knowledge-repo.md`](./reference/knowledge-repo.md) | the repository this platform reads and writes: the three zones, the fast lane's three creatable types, the optional `ops/` files, and what the platform will never do to it. **Start here if you are setting one up** |
| [`reference/hybrid-index.md`](./reference/hybrid-index.md) | `stigmergy.index` — the hybrid derived index, the ranking batch, and the `stigmergy-index --check` substrate lint |
| [`reference/server.md`](./reference/server.md) | `stigmergy.server` — the single MCP server and its **ten** tools: stdio + streamable HTTP, auth, audit, rate limiting |
| [`reference/answer.md`](./reference/answer.md) | `stigmergy.answer` — the answering agent + strict verifier; three agent tools |
| [`reference/capture.md`](./reference/capture.md) | `stigmergy.capture` — the durable capture queue, attribution, the evidence plane, retention, the human loop |
| [`reference/librarian.md`](./reference/librarian.md) | `stigmergy.librarian` — the filing engine: the worker, the agent, the nine gates, the commit, the entity proposals it writes |
| [`reference/meeting-distiller.md`](./reference/meeting-distiller.md) | the librarian's meeting flow: a transcript submitted at `brain_submit(kind="meeting", …)` becomes a page SET (source + meeting + N decisions), filed atomically with per-page anchoring |
| [`reference/views.md`](./reference/views.md) | `stigmergy-views` + `stigmergy.views`: a derived, per-entity rollup — a deterministic skeleton (timeline, backlinks) plus an agent-written synthesis, `acl` the intersection of its members' audiences, regenerated after a meeting files or on demand |
| [`reference/slack.md`](./reference/slack.md) | `stigmergy.slack` — the third transport: identity resolution, the `@brain`/DM ask, the 🧠 capture gesture, the push-channel poller, the steward doorbell and the Block Kit review surface |
| [`reference/navigation.md`](./reference/navigation.md) | `stigmergy.server` — the graph served (`read_page`'s `links`/`backlinks`), `list_entities`/`describe_entity`, entity-first resolution in the service layer so every client inherits it |
| [`reference/gardener-digest.md`](./reference/gardener-digest.md) | `stigmergy.gardener` + `stigmergy.digest` — `stigmergy-gardener`'s **ten** deterministic corpus-health checks plus a bounded model editorial sweep, findings persisted and reported, plus an SLA Slack notice that cannot fire today (nothing produces an `sla` finding); `stigmergy-digest`'s two-section Slack post, ACL-scoped at the destination channel, `--dry-run` byte-identical. The digest is a command, not a cron; the daily cron runs the gardener alone |
| [`reference/repair.md`](./reference/repair.md) | `stigmergy.repair` — the governed repair loop: `stigmergy-repair propose` turns the four answerable gardener findings into additive edits and drafted entity bodies, `brain_delete` (MCP, and the console's own button) is where a PERSON removes pages — decided and applied in the same call, since the judgment was theirs, with the pages that referred to them rewritten by a model and the diff handed back; a steward approves the model-initiated proposals one at a time over MCP or in the console, and code applies exactly those ops through the librarian's own gates as one App-authored commit. A rejected proposal is the dismissal memory; there is no `apply` command and no `delete` one |
| [`reference/admin-console.md`](./reference/admin-console.md) | `stigmergy.admin` — the ops console at `/admin`, mounted on the SAME app process group rather than a fourth service: the steward drain, remote control of the four crons, gardener/repairs/digest/index panels and an activity view. One bearer credential, minted by `stigmergy-admin-token`, stored only as a hash; INERT until `STIGMERGY_ADMIN_TOKEN_HASH` is configured |
| [`reference/page-contract.md`](./reference/page-contract.md) | the FAST-LANE anchor rule: what `entity:` means, who writes it, how it is read |
| [`reference/brain-page-contract.md`](./reference/brain-page-contract.md) | the wider frontmatter dialect of pages already in the repo — a READ contract, since nothing writes it any more |
| [`reference/operator-runbook.md`](./reference/operator-runbook.md) | operator runbook: deploy, tokens, audit trail, object-store smoke check, the two databases, the librarian + its GitHub App, the Slack transport, the steward doorbell, the gardener cron and the digest command |

## Code maps (where things live)

Thirteen per-package maps live **beside the code**, one per package, in the standard
Purpose / Key entry points / Use these / Avoid / Data & contracts / Tests / Common tasks shape:

[`kernel`](../src/stigmergy/kernel/index.md) ·
[`index`](../src/stigmergy/index/index.md) ·
[`server`](../src/stigmergy/server/index.md) ·
[`answer`](../src/stigmergy/answer/index.md) ·
[`capture`](../src/stigmergy/capture/index.md) ·
[`librarian`](../src/stigmergy/librarian/index.md) ·
[`entities`](../src/stigmergy/entities/index.md) ·
[`views`](../src/stigmergy/views/index.md) ·
[`slack`](../src/stigmergy/slack/index.md) ·
[`gardener`](../src/stigmergy/gardener/index.md) ·
[`repair`](../src/stigmergy/repair/index.md) ·
[`digest`](../src/stigmergy/digest/index.md) ·
[`admin`](../src/stigmergy/admin/index.md) ·
plus [`evals/index.md`](../evals/index.md) for the measurement rig.

Two packages have a code map and **no** `reference/` page of their own. `stigmergy.kernel` has none
deliberately: it is a library other packages import — the model dispatch, the page contract's
constants and emitter, the ACL resolver, and the entity registry — with no operator surface and no
narrative to tell. `stigmergy.entities` does have an operator surface, and its narrative lives
under other titles: the runbook's
[governing what the librarian proposed](./reference/operator-runbook.md#governing-what-the-librarian-proposed)
section is where governed entity birth is operated from, the proposing half of
[`reference/librarian.md`](./reference/librarian.md) is where it is triggered, and
[ADR 016](./decisions/016-human-loop-and-entity-governance.md) with
[ADR 041](./decisions/041-file-first-govern-after.md) are the design record.

## Decisions

**On the numbering.** The surviving records start at 006, and there are gaps: no 001–005, 009, 011,
019 or 025. Eight of those were **deleted, not superseded**, when their subjects were removed from
the system whole: 002 (ingest-time deterministic verification), 003 (the ingest worker's bounded
agency), 004 (the pipeline supervisor and its agent memory), 005 (the facts layer), 009 (the first
rollup design, superseded by 021), 011 (the connector source contract), 019 (the canon lane) and
025 (the fleet supervisor and its playbook). Each one's subject was removed from the system whole,
so the record went with it rather than being kept as a description of something that is not there.

Nothing was renumbered to close a gap, because a stable ADR number is worth more than a tidy
sequence: a reference to "ADR 015" in a commit message or a code comment should still resolve years
later. [ADR 026](./decisions/026-the-purge.md) and
[ADR 027](./decisions/027-the-contraction.md) record the two removals that account for most of them.

Writing one is [`CONTRIBUTING.md`](../CONTRIBUTING.md#writing-a-decision-record), which states the
above as rules to follow rather than as the history that produced them.

| ADR | Decides |
|---|---|
| [006](./decisions/006-time-semantics.md) | provable `as_of`, near-duplicate lineage, supersedes chains |
| [007](./decisions/007-answer-layer.md) | the answer path is owned server-side: one MCP server enforces the contract and ACLs |
| [008](./decisions/008-entity-registry.md) | entity resolution is path-based and deterministic, plus a curated registry |
| [010](./decisions/010-acl.md) | ordered path rules produce audience labels; malformed config fails loudly |
| [012](./decisions/012-hybrid-index.md) | the hybrid derived index: Postgres+pgvector, RRF, explainable contract ranking, wipe-and-rebuild |
| [013](./decisions/013-http-transport-and-token-auth.md) | streamable HTTP transport, per-user hashed-token auth, mandatory stateless mode, cloud staging deploy |
| [014](./decisions/014-capture-queue-and-attribution.md) | the durable capture queue and its single writer, server-side attribution that cannot be forged, the content-addressed evidence plane, retention |
| [015](./decisions/015-librarian.md) | the librarian: the agent judges and code vetoes the diff, an ephemeral worktree as the blast radius, declared-not-performed edits, a figure that cannot be traced bounces rather than shipping behind a banner, the App as the committer |
| [016](./decisions/016-human-loop-and-entity-governance.md) | the human loop and governed entity birth: the librarian asks back instead of guessing, a steward approves an entity before it exists, the registry as a derived view |
| [017](./decisions/017-slack-transport.md) | the Slack transport: in-process (not over HTTP), the channel scopes the thread and the asker gets the rest by DM, the 🧠 gesture captures verbatim, five identity outcomes, a pure renderer that never trusts `confidence` |
| [018](./decisions/018-pilot-readiness.md) | incremental index upsert on merge via a narrowly-scoped webhook exception, the withheld excerpt closes when the librarian has looked (not at a terminal state), immediate purge on a secret/PII rejection, a refusal's prose composed by the server from structured facts (never the model), entity-first retrieval (hard-scoped then; a rank-time boost since ADR 022 D4's amendment), `audit_log.result` as a per-tool summary |
| [020](./decisions/020-meeting-distiller.md) | the meeting distiller: a second flow rather than a branch, one capture filing an atomic page SET where the write path had always filed exactly one page; `kind` is the material's shape and the flow that reads it, validated at the enqueue seam every door crosses; anchoring goes per decision page because a meeting's decisions belong to different entities; flow policy is passed into `GateContext` and every default is closed |
| [021](./decisions/021-views.md) | views: a derived per-entity rollup, members read from the repo and never the index; the skeleton must not wait on the synthesis; the audience rule needs TWO gates — the member intersection AND a separate read gate for the non-member sources (backlinks) that also feed the page; one commit per entity, diverging from ADR 020's atomic page set because entities share no invariant; the withheld synthesis is prose with a stated reason, never a new enum value; a derived view's failure never taints the fact it derives from |
| [022](./decisions/022-entity-navigation.md) | entity navigation: the graph (`links`) becomes a served, ACL-scoped, first-class column computed once at build time, never re-derived from untrusted body text at read time; `read_page`'s links/backlinks ride the existing ACL-visibility check, filtered before the cap; entity-first resolution moves down into the service layer so every client inherits it, not just `ask`; `describe_entity` resolves through the registry or exact scoped-set membership — one existence rule, never a second resolver, closing a `list_entities`/`describe_entity` disagreement and a timing oracle |
| [023](./decisions/023-learning-loop.md) | **HISTORICAL — the subject was deleted whole** (superseded by ADR 027), kept because a rebuild would start here. `stigmergy.loop`, `loop_candidates`, staging and the `candidate` review kind exist nowhere in the code today. The reasoning that outlived it: `UserRef` is a type only hashing can construct; the capture hook lives at the tool-closure seam so a transport's own direct calls are never double-staged; staging is thread-scoped, not speaker-scoped; promotion is decide-and-enqueue in one transaction riding the ordinary fast lane, no new write path |
| [024](./decisions/024-gardener-digest.md) | the gardener and the digest: deterministic checks as the provable core plus a bounded model sweep for what only judgment sees; watermarks live in `job_runs.stats`, anchored to a captured instant rather than `started_at`; `job_runs.status` grows a third value, `partial`, so a failed sweep never poisons the watermark it would otherwise advance; the digest broadcasts, so it and the SLA notice are ACL-scoped at the destination channel, with the redaction residual named rather than hidden; `views/staleness.py` exists so an architecture test's "no git plumbing" claim is true rather than merely asserted; page selection runs outside the sweep's own failure boundary on purpose — and its FIRST AMENDMENT adds a second model pass, `model-empty-entity-body`, with its own prompt and its own population: entity pages are a bounded set, so that pass COVERS them rather than inheriting the editorial sweep's rotating sample, a page still carrying literal placeholders is excluded before the model is asked so the two entity-body checks can never name one page twice, its ceiling records what it deferred instead of truncating in silence, and a failure of either model pass — not only the sweep's — commits `partial` |
| [026](./decisions/026-the-purge.md) | **the purge**: the canon lane goes whole; ingest-time figure verification dies and answer-time verification becomes the whole of it (a field nothing computes is not stamped); the fleet supervisor and its playbook go, and `ops/` reverts to one path-scoped writer; the corpus-distillation pipeline becomes `stigmergy.kernel`, a library that imports nothing from this project, keeping the document converters the Drive door then ran; the eval floor becomes the two real golden instruments over a frozen corpus |
| [027](./decisions/027-the-contraction.md) | **the contraction**: the learning loop is deleted rather than left dormant — every one of its tentacles out of the live files, with ADR 023 holding the design if it is ever rebuilt; the `date-bearing-body-link` veto steps down to a gardener finding (`gardener.checks.check_date_bearing_body_links`, which reports and never vetoes); `meeting-links-mismatch` folds out, the mismatch class having become structurally impossible; the PII gate stays, on evidence |
| [028](./decisions/028-drive-door.md) | **HISTORICAL — the door and the converters behind it were replaced by ADR 044 D4**, kept because it is the record of where extraction was argued to belong. The Drive door: `stigmergy-drive` fetched through the operator's own Google auth, wrote evidence and ONE queue row, and called no model and performed no conversion — the conversion happened at the worker, over the evidence blob. Client-side extraction is what 044 answered differently; `kernel.doctools` was deleted with this door |
| [029](./decisions/029-admin-console.md) | the admin console: `/admin` on the existing app rather than a fourth process group; dispatch, never re-execute; one bearer credential minted by `stigmergy-admin-token`, stored only as a hash, with the console inert until that hash is set |
| [030](./decisions/030-server-side-entity-minting.md) | entity birth completes from all three interfaces rather than dead-ending in a command a human must run with a checkout — superseding ADR 029's "entity writes stay CLI": a server-side mint commits as the librarian App with an `Approved-by:` trailer naming the resolved human, and reuses `stigmergy.entities`' write discipline (collision checks, drift refusal, gitleaks, one commit, no force-push) rather than reimplementing it |
| [031](./decisions/031-suppression-gated-retry-and-cost-instrumentation.md) | `ask`'s corrective retry is spent only where the strict gate would suppress the draft (an untraced figure — the quote scan included — or a `failed` verdict), retiring the measured majority case that paid a full second run for a `partial`→`verified` label; the trigger and the gate share one scan (`strict_gate_findings`) so they cannot drift; and the spend reaches the row — the librarian's per-run SDK cost summed into `report.cost_usd`, `ask`'s token counts into `audit_log.result.usage` — so the next model-policy decision starts from recorded dollars |
| [032](./decisions/032-filing-port-and-pricing-seam.md) | the librarian's agent seam becomes a named `Protocol` (`filing_port.FilingAgent`) with three structurally conforming backends instead of a convention two modules shared; the usage contract goes tokens-first behind one pricing seam (a configured $/MTok table with an `AS_OF` date and an env override, and a LOUD refusal for an unpriced model rather than a silent `$0.00`); and the meeting flow — already structured and tool-less by ADR 020 — is the first onto pydantic-ai, refused for a worker at startup because a queue carries ordinary captures too. Expand only: nothing is retired until measured evidence and an explicit decision say so |
| [033](./decisions/033-structured-filing-flow.md) | the ORDINARY flow takes the meeting flow's shape: a deterministic gatherer (`librarian/gather.py`) reads the CHECKOUT at the base commit — never `pages_index`, so no ACL exception is needed for a write-path worker — and hands the model candidates, the resolved entity view, the link neighbourhood and the repo's wikilink vocabulary; the outcome envelope grows an ADDITIVE `page` object carrying the page's own text (refused over its ceiling, never truncated, because a page body is the product); CODE writes the page and confinement gets STRONGER (no tool to confine, no field that can name a path); the brief becomes backend-neutral and the SDK carries the named override; and `backend=pydantic` serves a worker, retiring M1's meeting-only refusal. **Amended: the gate D6 named was spent — golden all-bars-PASS on the structured flow, a staging shakedown, the container e2e on CI, then an explicit decision — and the `sdk` ordinary path is retired.** A deployment still configured for it is refused at startup by name, with the two edits the replacement takes and the image rollback |
| [034](./decisions/034-agentic-pydantic-harness.md) | the ORDINARY filing agent explores again, on THIS project's harness: five tools registered on the pydantic-ai `Agent` inside `run()` whose bodies are the gatherer's own pure functions (`search_pages`, `read_page`, `list_page_names`, `resolve_entities`, `write_page`), with confinement asked INSIDE each tool rather than in a permission hook; ADR 033's gathered block survives as the SEED the tools go further from, so `wants_gathered` joins the port as a second declaration beside `structured_ordinary`; the account travels as the outcome FILE again and the backend drains its own channel; `max_turns` is un-deprecated as the iteration budget (`UsageLimits(request_limit=…)`) while `max_tool_calls` stays deprecated because the framework counts tool calls itself; `turns`/`tool_calls` carry real numbers again, with zero now meaning "this shape has no loop"; the brief-matches-backend startup check is retired rather than re-aimed. The rule it records: **deterministic code may seed context and implement tools, and must not replace the model's ability to decide the context is not enough** |
| [035](./decisions/035-filing-reliability-brief-and-fault-visibility.md) | the ordinary agentic run's "Writing the page" brief section becomes one symmetric statement instead of one paragraph read as the default and one read as the exception, closing a measured class of body-only drafts with no frontmatter block (8/13 → 0/12 across a frozen fixture and a clone); the contract gate's `frontmatter` finding earns a shape-neutral facts line on top of its message regardless of which brief bytes drafted the page; a framework `UnexpectedModelBehavior` fault now persists its real message, bounded and fence-neutralized, on the log line and the corrective-retry Finding instead of surviving only as a bare class name, while a blanket exception handler's WIRE message stays class-only since it can catch a provider error carrying prompt text; a new `stigmergy.text.one_line` seam replaces two hand-rolled versions of the same sanitize-collapse-clamp pipeline; deterministic frontmatter construction by code and a platform-side override note are both rejected, and a tool-retry budget knob stays deferred until a persisted fault names which of two candidate causes it would address |
| [036](./decisions/036-librarian-prompt-caching.md) | Anthropic prompt caching on the ORDINARY filing run, ON by default: an iterating run resends the identical system prompt, tool schemas and gathered seed on every turn, and a cache read prices at a fraction of ordinary input, so `pydantic_backend.prompt_cache_settings` maps a configured `STIGMERGY_LIBRARIAN_PROMPT_CACHE` TTL onto the three Anthropic cache fields for an `anthropic:` model and nothing for any other id or `off`; the meeting flow stays byte-for-byte untouched, since one call means a cache write with no read to offset its own 1.25x premium; `pricing.PRICES` gains a fourth `cache write` column (ADR 032's own named follow-up), with a legacy three-figure `$STIGMERGY_LIBRARIAN_PRICING` override still accepted and normalized to the old write-at-input-rate semantics |
| [037](./decisions/037-second-brain-comparison.md) | the comparison against the reference personal-second-brain pattern, settled: **governed entity birth stays** — a multi-writer brain's vocabulary is agreed rather than coined, and lint-after-the-fact reconciliation is the shape that does not survive the change of audience, so the re-open trigger is a countable triage backlog and not a principle; **no `meta` page type** — operational state lives in Postgres, the admin console and the digest, because a corpus page is retrievable and `ask` answering a company question with the state of a gardener sweep is the failure mode; **no `project` PAGE type, `project` becomes the SEVENTH entity type** — an ongoing initiative is a governed identity that earns birth-by-steward and anchoring, and ADR 021's regenerated per-entity view is the living status page the reference pattern hand-maintains, so the anchored notes/decisions/meetings are its history and the view is its summary; and #33's frontmatter proposals field by field — `role` already exists, concept `domain`/`aliases` decline to tags plus hybrid retrieval, source `confidence` is the removed `verification` field's ghost, `author` is `submitted_by`'s unverifiable twin, `origin` is a pointer where the evidence store plus `content_hash` is a proof, and `note_type: decision` would demote a first-class page type to a label |
| [038](./decisions/038-meeting-distiller-corpus-context.md) | the meeting distiller stops filing blind: the WORKER hands it the same deterministic gathered context the ordinary flow gets (`gather.gather` + `render_gathered`'s no-tools defaults, fenced, above the transcript and below the registry), and the account may DECLARE additive edits in the ordinary flow's exact vocabulary — one shared `agent._parse_edits`, performed by `edits.apply_declared`, judged by `gate_body_rewrite` — so a meeting's decision pages stop being leaves. The flow stays tool-less and code still writes every page; `GateContext.edits_allowed=False` loses its last caller (field and branch kept, red proof moved to the gate unit, finding code kept because deployed refused diffs already name it); `report.filed_meeting`'s `pages_edited` stops being a hardcoded `(none)`; the meeting lane is NOT widened, so the editable set is `wiki/decisions/` and the brief says so. The one piece of new code is `_edits_with_resolved_links`, which maps a declared decision TITLE onto the slugified stem the worker filed it under — without it the agent's only name for the page it just described is a dead link that refuses the whole set and spends both attempts, observed before the fix. Deliberately absent: read tools for the meeting agent, and entity-page enrichment |
| [039](./decisions/039-governed-repair-loop.md) | a gardener finding gets a path to zero without anything gaining commit rights: a MODEL proposes a concrete edit, CODE validates it twice (at propose time against the checkout, at apply time against a fresh clone), a HUMAN approves ONE at a time, and code applies exactly the approved ops through `edits.apply_declared`, `run_gates(ALL_GATES)` and `gitcmd.commit(gated_entries=…)`. The op vocabulary is the librarian's three declared-edit kinds, so every additive repair is a shape the eight gates already judge — and its FIRST AMENDMENT adds `entity-body`, the one kind that replaces text: a drafted body for an entity page still carrying its template, admitted by a per-path permission the apply tells the gates and nothing else may set. Its SECOND adds `delete`, the one kind that removes anything and the one a model may never propose: a person types it, or code derives it for exact-duplicate `sources/` pages where the decision is a lookup rather than a judgment; the unit of approval is a SWEEP — the pages that go and the full planned bytes of every page that mentions one — recomputed at apply time and refused unless byte-identical, with the additive proof replaced by byte-equality and `wiki/entities/` undeletable by construction; a REJECTED row is the dismissal memory, keyed on what a proposal would DO rather than on which finding suggested it, so a steward who declines once is not asked again; the proposer's editorial procedure lives in the KNOWLEDGE repo and is read at run time (a missing skill is a named refusal, and the drift risk is accepted for v1); the review lane's third item kind is the first whose steward guard asks a per-TARGET-PATH question, because a repair is the first verdict that can land inside a delegated zone; and one apply ordering serves both approving doors, with a failed apply staying `failed` rather than silently returning to pending. No batch approval, no autonomous apply, no doorbell card, and no `stigmergy-repair apply` |
| [040](./decisions/040-ops-file-freshness.md) | the `ops/` control files a running process trusts ride the derived index as verbatim-TEXT snapshots — one relpath-keyed cache for the entity registry, the identity roster and the channel scope map, refreshed by the push webhook and reconciled per file by the nightly rebuild — so a revocation or a scoping edit lands within seconds of its push on process groups that hold no checkout, instead of at the next deploy. The webhook fetches ops files at the BRANCH ref (a replayed delivery can only install what the branch says now) and records applied delivery ids for the page road; absence is reconciled per file (the registry clears, the access files are KEPT — a cron must never restore a deploy-baked roster); an EMPTY snapshot is malformed and resolves nobody; the two registry parsers stay two, with authority split between writing (strict, `kernel.registry`) and serving (tolerant, `server/entity_aliases`); `ops/stewards.json` stays out, its deployed staleness a recorded pending decision |
| [041](./decisions/041-file-first-govern-after.md) | **file first, govern after**: a capture never waits on a person — the librarian's one decision is `file`, and a name the registry does not know becomes a PROPOSED entity (a complete page under `wiki/entities/` with `approved_by: ""` and a `proposed` registry entry) the capture anchors to in the same commit; `needs_input`, `triage`, the ask-back and `brain_reply` are retired, and rows found parked return to the queue once; the pending set is whatever the registry marks proposed and the inbox is DERIVED from registry + `pages_index` + ledger, so there is no second table and nothing to drain; three verbs — approve, merge into, decline (approve / decline for a proposed spelling) — through one governed writer (`entities.decide.apply`) whatever the door, App-authored with a `Decided-by:` trailer; the ledger is the librarian's memory of no, so a declined name is never proposed twice; a ninth gate, `identity`, proves every write in the identity zone is a declared proposal arriving unconfirmed, and a name the registry already knows becomes a proposed spelling rather than a twin; hand registration is born confirmed; proposed entities are visible in search, marked |
| [042](./decisions/042-an-entity-is-born-written.md) | **an entity is born written, and keeps being written**: there is no deterministic birth — `entities.mint` and the server-driven `mint_via_clone` are gone, and an entity is born through the librarian, from material, with its page written by a model that has the material and the brain in front of it; a steward's registration (*Register an entity*, `stigmergy-entities create --about`) is a CAPTURE carrying `register_*` hints that `brain_submit` refuses from every client, and the entity proposed under that name is born CONFIRMED by the steward with the ledger row written after the push; `birth.render_page` refuses a page with no What / Who, drops a section with nothing to say and strips the template's comments; the account gains `entity_updates`, appended under a registered page's own Facts and Connections and proved byte for byte, so the spine accretes; the brief looks before it writes |
| [043](./decisions/043-a-sweep-is-written.md) | **a sweep is written, and the hand that typed the deletion has already approved it**: the pages a deletion leaves behind are WRITTEN by a model — handed the doomed pages, every page that names one of them and the plan code made — while their frontmatter stays code's; code proves the bounds (the touched set is exactly the referencing set, each frontmatter byte-identical to its own scrub, each body keeping its title and neither growing nor being cut down, nothing referring to a going page afterwards, `expected_bytes` to the gates), retries once and refuses whole, with no deterministic fallback; and D1's third clause reads, for this kind, *a HUMAN decides — at the command when they gave it, in the inbox when a model did*: a person's deletion enters through the authenticated doors, runs the per-path steward guard over the full touched set in the act, lands in one pass against one clone and returns the diff, its row born `approved` and never pending, its diffs ACL-scoped and fenced, the CLI verb gone; B4's recompute is replaced by the per-page base hash plus the corpus scan for the one road that still waits (code-derived duplicates); no model proposes a deletion, nothing a model initiated applies itself, no preview-then-confirm, no fifth kind |
| [044](./decisions/044-the-capture-is-the-approval.md) | **the capture is the approval**: the human act is the capture (or the removal they asked for), and everything after it is corpus maintenance judged by the nine gates, never by a second person — an identity a capture introduces is born CONFIRMED by the submitter (`approved_by` = `submitted_by`, no `proposed` state anywhere, a spelling lands in `aliases:`, a `register` hint is accepted from every door); a repair is derived, validated, applied and recorded with its diff by the worker (one commit per repair, `content_key` the permanent memory, two ceilings per pass), so the review lane, the doorbell, the console's inbox and verdicts, `stigmergy-entities`, `stigmergy-repair` and `ops/stewards.json` go; the worker is the ONE writer — `brain_delete` queues a `delete` row for unrestricted identities and the diff lands in the row's report, the API process loses its git credential; every kind enters at `brain_submit` (`raw`, `page`, `meeting`, `document` — text in, provenance in hints, per-kind caps), so the drop CLIs, the converters and the OCR go; approval-before becomes visibility-after (the digest, the console's ledger, `git revert`); and the four crons become the worker's idle-only night shift, so the Actions templates and the admin GitHub token go |

## A note on vocabulary

Some of these documents predate the current layout, and the module set moved more than once. They
were written for a flat module set in a per-package project where there is now one repo root, and
for the zone names the repo carried before they became `wiki/` · `sources/` · `views/`. What used
to be `stigmergy.pipeline` is gone; the modules the living system still uses were re-homed to
`stigmergy.kernel`, which is the bottom of the stack and imports nothing from this project — a rule
enforced by `tests/test_architecture.py`, not by this paragraph.

Three whole subjects were removed, and are named here so that a stale sentence elsewhere is
recognizable as stale rather than as something to go looking for: **the facts store** (`facts.db`,
the `query_metrics` tool, ingest-time figure verification), **the canon lane** (`brain_propose`,
`brain_promote`, `canon_proposals`, canon PRs) and **the learning loop** (`stigmergy.loop`,
`loop_candidates`, staging). Nothing in `src/` implements any of them.
