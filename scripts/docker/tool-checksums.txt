# SHA-256 of every third-party binary the image downloads, in `sha256sum -c` format.
#
# The Dockerfile fetches exactly the one archive its build architecture needs and verifies it
# against this file with `sha256sum --ignore-missing -c` — which fails when a downloaded archive
# does not match, and also when NOTHING was verified (coreutils exits non-zero if no listed file
# was present, so a silently-skipped download cannot pass).
#
# Both architectures are listed because the image is built in two places: linux/amd64 for Fly, and
# whatever `docker compose build` is running on for the librarian e2e (an arm64 Mac, today).
#
# **The rule this file exists for, kept where the next entry will meet it: a version pin says WHICH
# release, a digest says which BYTES.** It was learned on an entry that is no longer here — the
# retired filing backend's agent CLI was version-pinned and installed straight from the npm registry
# for a while, so a rebuild trusted whatever the registry served for that version while the binaries
# beside it were digest-verified. Anything this image downloads gets a line here, whatever ecosystem
# it comes from.
#
# To bump a version: change it in the Dockerfile's ARG, then replace the line(s) here from upstream —
#   gitleaks:    https://github.com/gitleaks/gitleaks/releases/download/v<V>/gitleaks_<V>_checksums.txt
# Never hand-edit a digest: the point of the file is that nobody can.

551f6fc83ea457d62a0d98237cbad105af8d557003051f41f3e7ca7b3f2470eb  gitleaks_8.30.1_linux_x64.tar.gz
e4a487ee7ccd7d3a7f7ec08657610aa3606637dab924210b3aee62570fb4b080  gitleaks_8.30.1_linux_arm64.tar.gz
